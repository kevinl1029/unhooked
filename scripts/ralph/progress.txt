# Ralph Progress Log
Started: Mon Feb 16 23:19:21 EST 2026
---

## Codebase Patterns
- TypeScript types in types/database.types.ts must be updated manually (npm run db:types requires live Supabase connection)
- No tsc binary in project — use `node_modules/.bin/nuxi prepare` to verify types generate correctly
- Migration pattern: ALTER TABLE public.X ADD COLUMN IF NOT EXISTS y TYPE DEFAULT value;
- Nuxt's typecheck: `node_modules/.bin/nuxi prepare` generates types; no typecheck script available
- Available npm scripts: dev, build, preview, postinstall, test:unit, test:unit:watch, test:unit:coverage, test:e2e, test:e2e:ui, db:types
- Nuxt test env overrides vi.stubGlobal('useRuntimeConfig'): use optional secret param in utility functions for testability, or add env vars to .env.test
- For server utilities that read useRuntimeConfig, accept optional dep-injection params (e.g., `secret?: string`) to enable direct unit testing without mocking Nuxt's runtime config

## 2026-02-16 - US-003
- Created server/middleware/rate-limit.ts with in-memory IP-based rate limiting
- Limits 10 requests/60s per IP; only applies to /api/check-ins/open/, /api/check-ins/unsubscribe, /api/check-ins/resubscribe
- Extracts IP from x-forwarded-for header, falls back to socket.remoteAddress
- Returns 429 { error: 'Too many requests' } on limit exceeded
- Cleans up expired entries on each request to avoid memory leaks
- Typecheck passed; all 434 unit tests pass
- Files changed: server/middleware/rate-limit.ts
- **Learnings for future iterations:**
  - Nuxt server middleware in server/middleware/ auto-registers; no imports needed
  - Use getHeader(event, 'x-forwarded-for') and event.node.req.socket.remoteAddress for IP extraction
  - setResponseStatus(event, 429) + returning a JSON object sends the status + body correctly
---

## 2026-02-17 - US-002
- Created server/utils/auth/hmac-tokens.ts with generateUnsubscribeToken() and validateUnsubscribeToken()
- Added UNSUBSCRIBE_SECRET to .env.example with comment
- Added unsubscribeSecret to nuxt.config.ts runtimeConfig
- Added '/unsubscribe' to supabase.redirectOptions.exclude array
- Added UNSUBSCRIBE_SECRET test value to .env.test
- Created tests/unit/auth/hmac-tokens.test.ts with 6 passing tests
- Functions accept optional `secret` param for testability (Nuxt test env overrides vi.stubGlobal)
- Files changed: server/utils/auth/hmac-tokens.ts, .env.example, .env.test, nuxt.config.ts, tests/unit/auth/hmac-tokens.test.ts
- **Learnings for future iterations:**
  - Nuxt's test environment provides its own useRuntimeConfig that vi.stubGlobal cannot override
  - Workaround: add optional secret/dep-injection params to server utils, or set env vars in .env.test
  - The optional param approach keeps the production API clean while enabling direct unit testing
---

## 2026-02-17 - US-001
- Created supabase/migrations/20260216_email_unsubscribe.sql with ADD COLUMN IF NOT EXISTS email_unsubscribed_at TIMESTAMP WITH TIME ZONE DEFAULT NULL
- Updated types/database.types.ts: added email_unsubscribed_at: string | null to user_progress.Row, email_unsubscribed_at?: string | null to Insert and Update
- **Learnings for future iterations:**
  - No tsc binary installed; use `node_modules/.bin/nuxi prepare` to check types generate without error
  - database.types.ts fields are alphabetically sorted within each section (Row, Insert, Update)
  - Project is on feature/checkin-email-enhancements branch; ralph branch was created fresh from that
---
