# Ralph Progress Log
Started: Sat Feb 21 21:26:59 EST 2026
---

## Codebase Patterns
- SQL migrations use `CREATE TABLE IF NOT EXISTS` and `ALTER TABLE ... IF EXISTS` patterns
- Migrations live in `supabase/migrations/` with YYYYMMDD prefix
- No FK constraints on analytics/telemetry tables (fire-and-forget, decoupled)
- RLS is enabled on all new tables with no anon policies (service_role bypasses)
- `nuxi typecheck` fails (no tsconfig.json); use `npm run build` to verify TypeScript
- `server/utils/` has flat and nested structure (llm/, tts/, prompts/, etc.) — place new top-level utils directly in `server/utils/`
- writeTo uses `.then().catch()` (NOT async/await) for true fire-and-forget telemetry writes

## 2026-02-21 - US-001
- Created `supabase/migrations/20260221_chat_telemetry.sql` with 25-column chat_telemetry table
- Table has no FK constraints on user_id, conversation_id, message_id (decoupled analytics)
- No DEFAULT on created_at — set by application
- Includes COMMENT ON TABLE + 8 COMMENT ON COLUMN statements
- 5 indexes: created_at, tts_provider, llm_provider, session_type, user_id
- RLS enabled, no anon policies
- Switched from feature/chat-telemetry to ralph/chat-telemetry branch
- Build passes (npm run build)
- **Learnings for future iterations:**
  - Branch was `feature/chat-telemetry`; had to create `ralph/chat-telemetry` from it
  - Migration pattern matches existing files: plain SQL, no rollback section
  - Spec has full SQL at lines 327-378 of docs/specs/chat-telemetry-spec.md
---

## 2026-02-21 - US-002
- Created `server/utils/telemetry.ts` with `ChatTelemetryCollector` class, `ChatTelemetryRow` interface, and `classifyTelemetryError()` function
- ChatTelemetryRow has all 25 fields matching DB schema with proper nullability
- CollectorInit interface has all 13 init fields
- Idempotency guards (firstTokenMarked, firstSentenceMarked, firstAudioMarked) prevent double-recording
- markStreamStart captures both streamStartTime (number) and stream_start_time (ISO string)
- writeTo uses .then().catch() (not async/await) for true fire-and-forget
- classifyTelemetryError checks error.status and error.message.toLowerCase() with specific patterns
- Build passes (npm run build)
- **Learnings for future iterations:**
  - `nuxi typecheck` fails without tsconfig.json; use `npm run build` instead
  - Spec implementation code is at lines 384-537 of docs/specs/chat-telemetry-spec.md
  - The `writeTo` method signature is `writeTo(supabase: any): void` — returns void, not Promise
---

## 2026-02-21 - US-003
- Created `tests/unit/telemetry/chat-telemetry-collector.test.ts` with 26 test cases
- Full success lifecycle test: all marks → correct ms values relative to stream start
- Error path test: only early marks → late timing fields remain undefined (not null)
- Idempotency tests for markFirstToken, markFirstSentence, markFirstAudio
- Timing math tests: duration_ms from requestStartedAt, ttft/ttfs/ttfa from streamStartTime
- writeTo fire-and-forget test: returns undefined (void)
- Supabase INSERT failure → console.error called with requestId
- Supabase INSERT success → no console.error
- All 10 classifyTelemetryError classification tests pass
- All 26 tests pass; pre-existing failures in check-in-sender.test.ts are unrelated
- **Learnings for future iterations:**
  - `ChatTelemetryCollector` uses `Partial<ChatTelemetryRow>` — unset timing fields are `undefined`, not `null`. Tests must use `toBeUndefined()` for fields never written, not `toBeNull()`
  - `vi.runAllTimersAsync()` needed after `writeTo` to flush the `.then().catch()` promise chain
  - Import tests use `~/server/utils/telemetry` path alias (matches existing test patterns)
  - 4 pre-existing failures in `check-in-sender.test.ts` — not caused by this story
---
