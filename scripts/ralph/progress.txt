# Ralph Progress Log
Started: Sat Feb 21 23:32:49 EST 2026
---

## Codebase Patterns
- The spec file for instant conversations v2 is at `docs/specs/instant-conversations-spec.md` (was on feature/instant-conversations branch, checked out to ralph branch)
- Typecheck uses `npx vue-tsc --noEmit -p .nuxt/tsconfig.json` (requires .nuxt to be generated first via `nuxt prepare`)
- Existing codebase has pre-existing type errors (not related to this feature) — "typecheck passes" means no new errors introduced
- SQL migrations follow pattern in supabase/migrations/: use `ADD COLUMN IF NOT EXISTS`, always include COMMENTs, use `ON CONFLICT DO NOTHING` for idempotent inserts
- `#supabase/server` is a virtual module that cannot be resolved in the nuxt unit test environment — added resolve alias in `vitest.config.ts` pointing to `tests/__mocks__/supabase-server.ts` stub
- H3 server utilities (`defineEventHandler`, `getHeader`, `readBody`, `createError`) are globals in Nitro server files but NOT in the nuxt test environment — use direct logic extraction pattern for unit testing server handlers
- Unit testing server API handlers: extract the business logic into a helper function in the test file that accepts dependencies as parameters (consistent with `check-in-sender.test.ts` pattern)

---

## 2026-02-22 - US-001
- What was implemented: Created database migration `supabase/migrations/20260222_instant_conversations_v2.sql`
  - Adds `precomputed_opening_audio_path TEXT DEFAULT NULL` to `user_progress`
  - Adds `precomputed_opening_word_timings JSONB DEFAULT NULL` to `user_progress`
  - Both have COMMENT ON COLUMN documentation
  - Creates `opening-audio` Storage bucket (private, not public) with ON CONFLICT DO NOTHING
  - Also checked out `docs/specs/instant-conversations-spec.md` from feature/instant-conversations branch
- Files changed: `supabase/migrations/20260222_instant_conversations_v2.sql`, `docs/specs/instant-conversations-spec.md`
- **Learnings for future iterations:**
  - The v2 spec is at `docs/specs/instant-conversations-spec.md` — line references in PRD notes match this file (e.g., "lines 426-449" for the migration SQL)
  - The branch `feature/instant-conversations` had the spec but not the implementation code for v2
  - Pre-existing type errors in the project don't block this work
---

## 2026-02-21 - US-002
- What was implemented: Created admin audio generation endpoint `server/api/admin/generate-opening-audio.post.ts`
  - Validates X-Admin-Secret header against ADMIN_API_SECRET env var (401 if missing/invalid)
  - Accepts optional illusionKey in body; generates for all 5 keys if omitted
  - Returns 400 for invalid illusionKey
  - For each key: synthesizes TTS, uploads to `opening-audio/l1/{key}.{ext}` (upsert: true)
  - Loads existing manifest from Storage, merges new results, uploads updated manifest
  - Failed keys keep their existing manifest entries (merge preserves them)
  - Uses serverSupabaseServiceRole() for all Storage operations
  - Logs with [admin-audio] tag
  - Added `adminApiSecret: process.env.ADMIN_API_SECRET` to nuxt.config.ts runtimeConfig
- Files changed: `server/api/admin/generate-opening-audio.post.ts` (new), `nuxt.config.ts`
- **Learnings for future iterations:**
  - `ILLUSION_OPENING_MESSAGES` is imported from `~/server/utils/prompts` — exported from `server/utils/prompts/index.ts`
  - `getTTSProviderFromConfig()` is in `~/server/utils/tts` — returns TTSProvider with `.synthesize({ text })` method
  - `TTSResult` has `audioBuffer: ArrayBuffer`, `contentType`, `wordTimings`, `estimatedDurationMs`, `timingSource`
  - `IllusionKey` type is imported from `~/server/utils/llm/task-types`
  - `serverSupabaseServiceRole` is imported from `#supabase/server`
  - Pre-existing unit test failures in `check-in-sender.test.ts` (4 tests) — not related to this feature
  - Nuxt auto-type route resolution errors in `useCeremony.ts` are pre-existing framework limitations, not new errors
---

## 2026-02-21 - US-003
- What was implemented: Created unit tests for admin audio generation endpoint `tests/unit/server/api/admin/generate-opening-audio.test.ts`
  - 17 tests covering: auth validation (401 for missing/wrong secret), key targeting (all 5 keys vs single key, 400 for invalid key), TTS generation and upload (correct path/extension, upsert:true), manifest management (merge, preserve failed key entries), and response shape
  - Tests use direct logic extraction pattern: the handler business logic is reimplemented as a testable helper function accepting dependencies as parameters
  - Added `tests/__mocks__/supabase-server.ts` stub for the `#supabase/server` virtual module
  - Added `resolve.alias` in `vitest.config.ts` to map `#supabase/server` to the stub file
- Files changed: `tests/unit/server/api/admin/generate-opening-audio.test.ts` (new), `tests/__mocks__/supabase-server.ts` (new), `vitest.config.ts`
- **Learnings for future iterations:**
  - `#supabase/server` requires a vitest resolve alias — see `vitest.config.ts` and `tests/__mocks__/supabase-server.ts`
  - Server API handlers can't be directly invoked in unit tests (H3 globals not available, virtual modules not resolved) — use the direct logic extraction pattern instead
  - The direct logic extraction pattern: copy the handler's business logic into a helper function in the test, inject dependencies as parameters (TTS provider, storage, config) — this is consistent with how `check-in-sender.test.ts` tests work
  - Mock Storage: create an object `{ from: vi.fn().mockReturnValue({ upload: vi.fn(), download: vi.fn() }) }`
  - Mock TTS manifest blob: return `{ text: () => Promise.resolve(JSON.stringify(data)) }` for the download response
---
