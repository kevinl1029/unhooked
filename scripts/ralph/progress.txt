## Codebase Patterns

# Ralph Progress Log
Started:
---
## 2026-02-08 22:21 - US-001
- Implemented database migration for ceremony v2
- Added ceremony_ready_at and ceremony_email_sent_at columns to user_progress table
- Added generation_status column to ceremony_artifacts table with CHECK constraint
- Updated types/database.types.ts to reflect new columns in both tables

Files changed:
- supabase/migrations/20260208_ceremony_v2.sql (created)
- types/database.types.ts (updated Row, Insert, Update types for ceremony_artifacts and user_progress)

**Learnings for future iterations:**
- Migration pattern: Use `ADD COLUMN IF NOT EXISTS` for safe migrations that can run multiple times
- CHECK constraints: Use DO block to add constraints conditionally (prevents errors on re-run)
- Type patterns: Database types follow Row/Insert/Update structure with nullable fields having `| null` in Row and `?: type | null` in Insert/Update
- Build command `npm run build` serves as typecheck for Nuxt projects (no dedicated typecheck script)
- Comments in migrations improve maintainability and explain the purpose of each change
---
## 2026-02-08 17:30 - US-002
- Implemented ceremony layout (immersive mode) and exit dialog
- Created layouts/ceremony.vue with minimal full-viewport layout (no header/navigation)
- Created components/CeremonyExitDialog.vue with accessibility features (alertdialog role, aria-modal, focus trap)
- Updated pages/ceremony.vue to use ceremony layout and added Escape key listener for exit dialog
- Dialog has proper focus management (Stay button gets default focus, Tab/Shift+Tab cycles between buttons)

Files changed:
- layouts/ceremony.vue (created)
- components/CeremonyExitDialog.vue (created)
- pages/ceremony.vue (updated - added layout metadata, Escape listener, exit dialog wiring)

**Learnings for future iterations:**
- Nuxt layouts: Use `definePageMeta({ layout: 'ceremony' })` to override default layout for specific pages
- Focus trap pattern: Watch the `open` prop to focus default element when dialog opens, use Tab keydown handler to cycle focus between elements
- Accessibility: Use `role="alertdialog"`, `aria-modal="true"`, and `aria-labelledby` for proper dialog semantics
- Vue event listeners cleanup: Always remove document event listeners in `onUnmounted` to prevent memory leaks
- Glass card styling: Use `glass` class with `rounded-card`, `shadow-card`, and `border-brand-border` for consistent brand styling
---
## 2026-02-08 23:45 - US-003
- Implemented ceremony system prompt builder
- Created server/utils/prompts/ceremony-prompt.ts with buildCeremonySystemPrompt() function
- Defined CeremonyContext interface with userStory, momentsByType, illusionsCompleted, productType
- Implemented complete 7-part ceremony flow with all dialogue scripts and emotional arcs
- Added token protocol instructions for JOURNEY_GENERATE, RECORDING_PROMPT, SESSION_COMPLETE
- Product type personalization with fallback to "nicotine products"
- Rationalization quote contrast in Part 5 with fallback when none captured
- External motivation handling in Part 3 (validates but redirects to internal)
- Not-ready exit handling in Part 4 (graceful exit without completion token)

Files changed:
- server/utils/prompts/ceremony-prompt.ts (created - 449 lines)

**Learnings for future iterations:**
- Prompt builder pattern: Export function that accepts context object and returns string prompt
- Long prompts: Use template literals with clear section separators (---) for readability
- Dynamic sections: Build Part 5A/5B as functions that inject product type and rationalization quotes
- Token protocol: Tokens are control signals stripped from display/TTS - document placement clearly in prompt
- Fallback handling: Always provide general framing when personalized content (quotes, product type) is unavailable
- Prompt instructions for flexible AI: Use phrases like "guided but flexible" and "exit criteria" to give AI decision-making power within structure
- Context interface: Match database Row types from types/database.types.ts for type safety
---
## 2026-02-08 - US-004
- Implemented /api/chat ceremony integration with token detection
- Added ceremony-specific branch in chat.post.ts that calls buildCeremonySystemPrompt() when sessionType='ceremony'
- Ceremony prompt populated with full user context: user_story (origin_summary, personal_stakes, primary_triggers), captured_moments grouped by type (rationalizations, insights, breakthroughs, observations), illusions completed list (all 5), and product_type from user_intake
- Created reusable generateJourneyArtifact() utility function in server/utils/ceremony/generate-journey.ts
- Journey generation utility handles full lifecycle: creates artifact with 'generating' status, fetches moments, selects best moments via AI, generates narrative, updates to 'ready' status, or marks 'failed' on error
- Token detection in both streaming (onComplete handler) and non-streaming response branches
- [JOURNEY_GENERATE] token triggers fire-and-forget call to generateJourneyArtifact() without blocking SSE stream
- All ceremony tokens ([JOURNEY_GENERATE], [RECORDING_PROMPT], [SESSION_COMPLETE]) naturally passed through in SSE stream for client-side detection
- Ceremony conversations automatically stored with session_type='ceremony' via existing conversation creation logic

Files changed:
- server/api/chat.post.ts (added ceremony branch, token detection, journey generation trigger)
- server/utils/ceremony/generate-journey.ts (created - reusable journey generation utility)

**Learnings for future iterations:**
- Chat endpoint structure: Session-type specific branches after message processing, before conversation creation (lines 82-246)
- System prompt pattern: Each session type builds its own system prompt and prepends as first message
- Token detection pattern: Check response text in onComplete handler, act on tokens, then pass through tokens naturally in SSE stream (client strips for display)
- Fire-and-forget background tasks: Use .catch() on Promise without await to trigger async work without blocking response
- Ceremony context fetching: Query user_story, captured_moments, and user_intake in parallel, group moments by type client-side
- Journey artifact generation: Extracted reusable function from existing endpoint, handles status transitions (pending → generating → ready/failed)
- Error handling for background tasks: Always log errors but don't throw - background failures shouldn't break the conversation
---
## 2026-02-08 - US-005
- Implemented background journey artifact generation
- Refactored generate-journey.post.ts to use reusable generateJourneyArtifact() utility function
- Updated GET /api/ceremony/journey endpoint to include generation_status in response
- Journey endpoint now returns { journey: { ... }, status: 'pending' | 'generating' | 'ready' | 'failed' }
- Reusable utility already existed from US-004 at server/utils/ceremony/generate-journey.ts
- POST endpoint simplified to call the utility and return status for retry scenarios

Files changed:
- server/api/ceremony/generate-journey.post.ts (refactored to use utility)
- server/api/ceremony/journey.get.ts (updated to include generation_status and wrap response in { journey, status } format)

**Learnings for future iterations:**
- Journey generation utility pattern: Reusable function already created in US-004 handles full lifecycle (pending → generating → ready/failed)
- GET endpoint pattern: Updated existing journey.get.ts to include generation_status field for polling
- Response format: Journey endpoint returns { journey: {...}, status: string } to support polling for generation progress
- POST endpoint purpose: Now acts as a retry mechanism (callable from dashboard) rather than primary generation path
- Fire-and-forget trigger: The /api/chat endpoint (US-004) calls generateJourneyArtifact() in background when [JOURNEY_GENERATE] token detected
- Status transitions: pending (created) → generating (LLM working) → ready (metadata saved) or failed (error occurred)
- TTS happens after 'ready': Audio generation per segment doesn't block 'ready' status
---
## 2026-02-08 - US-006
- Implemented ceremony complete endpoint v2 with cheat sheet generation
- Updated endpoint to require conversation_id (required) and accept optional final_recording_path
- Changed validation to check program_status='completed' instead of ceremony_completed_at
- Updated completion logic to set program_status='completed' (critical status change) and ceremony_completed_at timestamp
- Removed v1 artifact existence requirements (journey and final_recording no longer required to complete)
- Integrated existing cheat sheet generator (generateIllusionsCheatSheet, saveCheatSheetArtifact) from server/utils/ceremony/cheat-sheet-generator.ts
- Cheat sheet automatically generated at ceremony completion with all 5 illusions (stress_relief, pleasure, willpower, focus, identity)
- Each illusion includes static content (name, illusion, truth) plus optional userInsight from captured moments via user_story key_insight_id references
- Cheat sheet generation failures are logged but don't block ceremony completion (graceful degradation)
- Updated response format to { status: 'completed', ceremony_completed_at: string, journey_artifact_status: 'ready'|'generating'|'pending' }
- Journey artifact status fetched from ceremony_artifacts.generation_status for polling/UI support
- Removed v1 response structure with artifacts map and follow_up_scheduled details
- Typecheck passed via npm run build

Files changed:
- server/api/ceremony/complete.post.ts (refactored for v2)

**Learnings for future iterations:**
- Ceremony complete v2 philosophy: Completion should not be blocked by artifact generation status - let artifacts generate async
- The cheat sheet generator utility already existed and handles the full lifecycle (fetch insights, build entries, save artifact)
- program_status='completed' is the source of truth for ceremony completion (not ceremony_completed_at alone)
- Response format simplified for v2: return status fields needed by client, not full artifact objects
- Journey artifact generation happens in background during Parts 6-7 via [JOURNEY_GENERATE] token trigger (US-004)
- Cheat sheet generation uses try/catch with logging - ceremony completes even if cheat sheet fails
- The 5 illusion keys match database column prefixes: stress_relief, pleasure, willpower, focus, identity
- User insights are optional per illusion - only included if user_story has a key_insight_id and the moment exists
---
## 2026-02-08 - US-007
- Implemented SessionView ceremony token detection and pause/resume functionality
- Added @recording-prompt event emission when [RECORDING_PROMPT] token is detected in AI messages (same pattern as [SESSION_COMPLETE])
- Exposed pause() and resume() methods via defineExpose for ceremony orchestration
- Created isPausedByCeremony ref that disables mic button and text input when true
- Stripped [RECORDING_PROMPT] and [JOURNEY_GENERATE] tokens from displayed message text (added to existing [SESSION_COMPLETE] stripping)
- Updated server/utils/tts/sanitize.ts to add ceremony tokens to SYSTEM_TOKENS array (prevents them from being spoken)
- All input controls (mic button, text input, send button) respect isPausedByCeremony state via conditional rendering and disabled attributes
- Existing [SESSION_COMPLETE] detection and handling unchanged (preserves backward compatibility)

Files changed:
- components/voice/SessionView.vue (added recordingPrompt emit, pause/resume methods, ceremony token stripping, pause state management)
- server/utils/tts/sanitize.ts (added [RECORDING_PROMPT] and [JOURNEY_GENERATE] to SYSTEM_TOKENS)

**Learnings for future iterations:**
- Token detection pattern: Watch messages array with deep:true, check last message content.includes('[TOKEN]'), emit event on detection
- defineExpose pattern: Define methods as const arrow functions, then call defineExpose({ method1, method2 }) at end of <script setup>
- Ceremony pause state: Use a separate isPausedByCeremony ref distinct from isProcessing to control input availability during ceremony recording
- Token stripping: Apply .replace() chains in computed displayMessages for display AND add to SYSTEM_TOKENS in sanitize.ts for TTS
- Conditional rendering for paused state: Add v-if="!isPausedByCeremony" to voice controls, :disabled="isPausedByCeremony" to input elements
- The message watcher fires when content changes, so detecting tokens immediately as they arrive from SSE stream
---
## 2026-02-08 - US-008
- Implemented CeremonyRecordingInline component with complete recording lifecycle
- Created 5 visual states: idle (ready to record), recording (with timer), preview (playback controls), saving (upload), saved (confirmation)
- Added text input fallback state for users who prefer typing or have recording issues
- Integrated MediaRecorder via existing useAudioRecorder composable
- Implemented 5-minute auto-stop timer with elapsed time counter displayed during recording
- Upload logic with 3-retry exponential backoff (1s, 2s, 4s delays)
- localStorage fallback: saves blob as base64 data URL to 'unhooked:pending-ceremony-upload' key when all retries fail
- Client-side validations: 10MB blob size check with user-friendly error message
- Server-side validations: MIME type whitelist (audio/webm, audio/ogg), 10MB file size limit
- Auto-switch to text input if MediaRecorder throws (e.g., device storage full)
- Accessibility: ARIA labels for states (idle='Ready to record', recording='Recording in progress', saved='Recording saved')
- Animations: slide-in/out with prefers-reduced-motion support
- Emits @audio-saved(path), @text-saved(text), @error(message) for ceremony orchestration

Files changed:
- components/CeremonyRecordingInline.vue (created - 422 lines)
- server/api/ceremony/save-final-recording.post.ts (added MIME type and file size validations)

**Learnings for future iterations:**
- Recording component pattern: Use state machine with clear visual states (idle, recording, preview, saving, saved)
- Upload retry pattern: Exponential backoff with for-loop (1s, 2s, 4s) is simple and effective for 3 retries
- localStorage fallback schema: Must match what US-017 expects for retry queue (userId, blobDataUrl, timestamp, retryCount)
- MediaRecorder error handling: Watch recorderError ref and auto-switch to text input on failures (graceful degradation)
- Client-side size validation: Check blob.size before upload to give immediate feedback (blob.size / 1024 / 1024 for MB)
- Server-side validations: Always validate MIME type and file size server-side even if client validates
- Blob to base64: Use FileReader with readAsDataURL() wrapped in Promise for async conversion
- Animation accessibility: Check window.matchMedia('(prefers-reduced-motion: reduce)').matches to skip animations
- Component lazy loading: defineAsyncComponent should be used in parent (pages/ceremony.vue) not in component itself
- Timer cleanup: Always clear intervals and timeouts in cleanup function and onUnmounted
---
## 2026-02-08 - US-009
- Implemented useCeremony() orchestrator composable with full ceremony lifecycle management
- Created composables/useCeremony.ts exporting reusable ceremony orchestration logic
- Ceremony phase state machine: pre-ceremony → conversation → recording → completing → transitioning → (dashboard navigation)
- Error handling: transitions to 'error' phase on prepare failure or completion failure after retries
- Exponential backoff retry logic: retryWithBackoff() function with configurable attempts and delays (1s, 2s, 4s for 3 retries)
- Recording flow: handleRecordingPrompt() pauses conversation, handleRecordingSaved()/handleTextSaved() resume
- Completion flow: handleSessionComplete() calls /api/ceremony/complete with retries, sets ariaAnnouncement, auto-navigates after 5s
- Accessibility: respects prefers-reduced-motion (immediate navigation if set, 5s delay otherwise)
- Exit dialog integration: handleEscapeKey() toggles, handleLeave() navigates, handleStay() dismisses
- TypeScript interfaces: CeremonyPhase union type, CeremonyPrepareResponse, CeremonyCompleteResponse
- Prepare endpoint integration: validates ready===true, stores conversationId (or active_conversation_id for concurrent tabs)
- Reactive refs: ceremonyPhase, conversationId, isRecording, isCompleting, isTransitioning, showExitDialog, error, ariaAnnouncement, finalRecordingPath
- Build passed (npm run build)

Files changed:
- composables/useCeremony.ts (created - 221 lines)

**Learnings for future iterations:**
- Composable orchestration pattern: Manages lifecycle and coordinates between components (does NOT own their state)
- Phase-based state machine: Use union type for phase and transition functions to move between phases
- Exponential backoff utility: retryWithBackoff<T>(fn, maxRetries, baseDelay) - reusable for any async operation
- TypeScript response types: Define interfaces for API responses inline in composable for type safety
- prefers-reduced-motion: window.matchMedia('(prefers-reduced-motion: reduce)').matches for accessibility
- Ceremony prepare endpoint: Returns ready boolean, active_conversation_id (for concurrent tabs), and user context
- Ceremony complete endpoint: Requires conversation_id, accepts optional final_recording_path
- The composable exposes ALL state as refs (no internal state) for template reactivity and testability
- Navigation pattern: Use navigateTo() for client-side routing in Nuxt
---
## 2026-02-08 - US-010
- Implemented ceremony page pre-ceremony screen and route guards
- Added program_status-based route guards (redirect if completed or not ceremony_ready)
- Built pre-ceremony screen with "YOUR FINAL SESSION" eyebrow, 15 minutes instruction, quiet space instruction, product-specific text with fallback
- Product type fetched from /api/intake with friendly display mapping (vape, cigarettes, etc.)
- Removed entire v1 ceremony wizard (intro, generating, journey, recording, cheatsheet, complete steps)
- Reduced ceremony.vue from 746 lines to 193 lines (73% reduction)
- Pre-ceremony screen uses glass card styling with animate-fade-in-up animation
- Uses ceremony layout (immersive mode, no header)
- Escape key listener for exit dialog preserved
- Typecheck passed via npm run build

Files changed:
- pages/ceremony.vue (complete refactor - removed v1 wizard, added pre-ceremony screen with route guards)

**Learnings for future iterations:**
- Route guards pattern: Check program_status in onMounted, use navigateTo('/dashboard', { replace: true }) for immediate redirects
- Product type display: user_intake.product_types is an array, map to friendly names and join with "or" for display
- Pre-ceremony eligibility: program_status === 'ceremony_ready' is the source of truth (not ceremony_completed_at)
- Graceful degradation: If API calls fail, show the pre-ceremony screen rather than blocking (user might be eligible)
- The v1 wizard was deeply coupled to artifact generation timing - v2 moves all that to background during conversation
- Ceremony page will be extended in US-011 with voice conversation and inline recording (builds on this foundation)
---
## 2026-02-08 - US-011
- Implemented ceremony page voice conversation, recording, and completion wiring
- Complete ceremony v2 lifecycle orchestration with useCeremony() composable
- Lazy-loaded CeremonyRecordingInline via defineAsyncComponent (only loads when Part 6 reached)
- State-driven UI based on ceremonyPhase: pre-ceremony → conversation → recording → completing → transitioning → dashboard
- Wired SessionView events: @recording-prompt triggers pause + recording UI, @session-complete triggers completion + retry logic
- Implemented pause/resume with template ref to SessionView.pause()/resume() methods
- Added completing/transitioning phases with fade-out animation (respects prefers-reduced-motion)
- Error phase rendering with retry CTA and user-friendly error message
- ARIA live region for screen reader announcements (ariaAnnouncement ref from useCeremony)
- Exit dialog wired to useCeremony() handlers (Escape key toggles, Leave/Stay buttons)

Files changed:
- pages/ceremony.vue (complete refactor to v2 conversation-based ceremony)

**Learnings for future iterations:**
- Ceremony v2 philosophy: Single-page conversation flow, no multi-step wizard - state machine controls phases
- Phase-based UI: Use v-if on ceremonyPhase with one root element per phase for clean conditionals
- Template refs for exposed methods: `ref="sessionViewRef"` + `sessionViewRef.value?.pause()` to call child component methods
- Lazy-loading pattern: `defineAsyncComponent(() => import('~/components/X.vue'))` for components not needed at mount
- Event handler wrappers: Create local handlers (handleRecordingPromptEvent) that call both child methods and composable methods
- Transition component: Use Vue's <Transition> with Tailwind classes for slide-in/out animations (check prefers-reduced-motion)
- Fade-out timing: Use :class binding with ceremonyPhase + prefersReducedMotion check for conditional animations
- ARIA live regions: Add empty div with aria-live="polite" bound to reactive announcement text
- E2E test expectations: v1 ceremony tests expect wizard UI ("the final step", "loading your ceremony progress") - v2 is conversation-based
---
